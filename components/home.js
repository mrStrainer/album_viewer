import React, { Component } from "react";
import { Text, View, StyleSheet } from "react-native";

const Home = () => {
	return (
		<View style={{padding:10}}>
			<Text style={{color:'#ccc', }}>Log in</Text>
		</View>
	);
}

export default Home;